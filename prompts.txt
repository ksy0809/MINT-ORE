Prompt P_init

Define the relation class between the subject and the object in the given sentence. The relation class you classify will be used as the ground-truth label for the supervised learning of a relation extraction model. Because a relation exists between the subject and the object, you must explicitly specify a new relation class. Use the content of the sentence as the primary criterion for determining the relation, but leverage background knowledge as much as possible. Do not output anything other than the relation class. Sentence: {sentence}, Subject: {subject}, Object: {object}, relation class:


Prompt P_bc

Your task consists of three stages. 1. Identify the relation between the subject and the object within the given sentence. 2. Determine whether the relation between the subject and the object belongs to the given candidate relation class. 3. Output 1 if it belongs, and 0 if it does not. Do not output anything other than the classification result (0 or 1). Now, review the sample below and perform the classification. Sentence: {sentence}, Subject: {subject}, Object: {object}, Candidate relation: {candidate_relation}


Prompt P_gen

All given samples belong to the same relation class. Your task: Decide whether the relation can be expressed using one of the existing relations below. If YES: existing relation: <extract_relation_from_the_list>. If NO: Create a new relation phrase (5 words or fewer). Rules: 1) If using an existing relation, you must copy it exactly from the list. 2) If creating a new relation, output only the phrase (no prefix). 3) Do not output anything else. Current candidate relations classes: {candidate_relation}, existing relation list: {integration_str}, Samples: {packed_samples}, Output (one line only):


Prompt P_pr

Define the relation class between the subject and the object in the given sentence. The relation class you generate will be read by another LLM and used to classify this sample into one of the predefined relation classes. Therefore, generate a relation phrase that is specific enough for another LLM to correctly infer the intended predefined relation. Do not generate an overly broad or generic phrase such as 'related to', 'associated with', or 'has connection with'. Use a concise but informative phrase that clearly describes how the subject is related to the object. Use the content of the sentence as the primary criterion for determining the relation, but leverage background knowledge when it is necessary and well-supported. Because a relation exists between the subject and the object, you must explicitly specify a relation class. Do not output anything other than the relation class. Sentence: {sentence}, Subject: {subject}, Object: {object}, relation class:
